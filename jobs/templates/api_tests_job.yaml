- job:
    name: api_tests
    desciption: "<h3 style='color: red;'>api tests runner</h3>"
    project-type: pipeline
    concurrent: true
    parameters:
    parameters:
      - text:
          name: "REFSPEC"
          default: main
          description: "Branch"
    pipeline-scm:
      scm:
        - api-tests-scm
      script-path: ./pipelines/api_tests.groovy
#      script-path: burlaka_jenkins/pipelines/api_tests.groovy
