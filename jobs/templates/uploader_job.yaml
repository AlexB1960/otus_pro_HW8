- job:
    name: jobs_uploader
    project-type: pipeline
    description: "Jobs uploader"
    parameters:
      - text:
          name: "REFSPEC"
          default: main
          description: "Branch"
    pipeline-scm:
      scm:
        - git-jenkins-jobs
      script-path: jenkins/jobs_uploader.groovy
#      script-path: burlaka_jenkins/pipelines/jobs_uploader.groovy
#      script-path: ./pipelines/jobs_uploader.groovy
