---

- scm:
    name: git-jenkins-jobs
    scm:
    - git:
        url: ssh://git@github.com/AlexB1960/otus_pro_HW8.git
        credentials-id: jenkins-git
        branches:
        - FETCH_HEAD
#        refspec: ${REFSPEC}